# platform

Cross-cutting infrastructure shared by every module: configuration loading, structured logging, health checks, and correlation-ID (request-tracing) middleware. See `docs/architecture/cross-cutting-architecture.md` and `docs/deployment/deployment-architecture.md` (observability/logging). No implementation code yet (Phase 1 scaffold only).
