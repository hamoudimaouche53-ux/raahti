org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin's incremental compiler computes relative paths between the pub
# cache (C:\...) and this project (D:\...); on Windows that throws
# "this and base files have different roots" when the two live on
# different drive letters. Disabling incremental Kotlin compilation is the
# documented workaround (https://youtrack.jetbrains.com/issue/KT-56610) —
# see docs/phase-3-implementation-log.md, "Foundation Hardening" entry.
kotlin.incremental=false
